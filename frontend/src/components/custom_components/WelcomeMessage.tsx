import React from 'react';

interface WelcomeMessageProps {
    imgSrc: string;
    text: string;
    className?: string;
}

const WelcomeMessage: React.FC<WelcomeMessageProps> = ({ imgSrc, text='', className = '' }) => {
    return (
        
    );
};

export default WelcomeMessage;
